// Get command line arguments
const (process.argv);

// Assign arguments to variables
const x= process.arg1;
const y= pricess.arg2;

// Compare the variables
const areEqual = x === y;

// Log the result
console.log(areEqual);
