// TODO: Define a Validate class with an isPassword() function that takes in a password parameter.
const savedPassword =`nose`
function isPassword =function(params){
    if (params.length>0 && prams==savedPassword){
        return false;
    }
    
}
// TODO: Export the Validate class from this file.
 validate.prototype.isPassword
 module.exports=validate;